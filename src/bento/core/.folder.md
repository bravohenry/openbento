# Folder: /src/bento/core

1. **"地位"**：Bento 卡片的原子级实现，定义卡片的视觉结构、尺寸系统和组合模式。这是整个 Bento 系统的基础层，其他模块都依赖于此。
2. **"逻辑"**：通过 Compound Component 模式提供 BentoCard 及其子组件（Icon, Title, Subtitle, Image, Overlay, Action, Content），通过 Context 传递尺寸和主题信息。
3. **"约束"**：所有计算必须幂等，严禁直接操作 DOM，必须使用 BentoSizeMap 定义的尺寸常量。

## 🧩 成员清单

- `BentoCard.tsx`：卡片主组件，Compound Component 模式
- `BentoCard.types.ts`：卡片相关类型定义
- `BentoCard.styles.ts`：卡片样式常量和 Tailwind 类
- `BentoContext.tsx`：卡片状态管理 Context
- `BentoSizeMap.ts`：尺寸映射表 (1x1, 2x1, 2x2...)
- `BentoSlots.tsx`：卡片内容插槽组件
- `useBentoCard.ts`：卡片交互逻辑 Hook

⚠️ **触发器**：一旦本文件夹新增或文件架构逻辑调整，请立即重写此文档。

