# Folder: /src/bento/widgets

1. **"地位"**：具体卡片类型的实现层，将业务逻辑封装为可复用的 Widget 组件，每个 Widget 对应一种卡片类型。
2. **"逻辑"**：接收配置数据，通过 BentoCard 渲染，实现特定业务逻辑（如链接跳转、图片展示、地图嵌入等）。
3. **"约束"**：所有 Widget 必须注册到 registry，必须基于 BentoCard 构建，严禁直接操作 DOM。

## 🧩 成员清单

- `link/`：链接卡片（LinkWidget）
- `text/`：文本卡片（TextWidget）
- `image/`：图片卡片（ImageWidget）
- `map/`：地图卡片（MapWidget）
- `section/`：标题卡片（SectionTitleWidget）
- `github/`：GitHub 卡片
- `spotify/`：Spotify 卡片
- `registry.ts`：Widget 注册表
- `types.ts`：Widget 类型定义

⚠️ **触发器**：一旦本文件夹新增或文件架构逻辑调整，请立即重写此文档。





