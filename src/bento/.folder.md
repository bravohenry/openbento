# Folder: /src/bento

1. **"地位"**：Bento 卡片系统的核心实现层，提供卡片渲染、网格布局、拖拽交互和编辑器功能。这是 OpenBento 的心脏，所有用户可见的卡片功能都在这里。
2. **"逻辑"**：接收 Widget 配置，通过 BentoCard 渲染，由 BentoGrid 管理布局，Editor 提供编辑能力，Dnd 处理交互。
3. **"约束"**：所有卡片必须基于 BentoCard 构建，严禁直接操作 DOM，必须通过 Context 管理状态。

## 🧩 成员清单

- `core/`：卡片原子组件（BentoCard, BentoContext, useBentoCard）
- `dnd/`：拖拽交互系统（DndProvider, Draggable, Droppable）
- `editor/`：编辑器 UI（EditorContext, EditorToolbar, WidgetEditOverlay）
- `grid/`：网格布局（BentoGrid, BentoCell, useGridLayout）
- `widgets/`：具体卡片类型（LinkWidget, TextWidget, MapWidget...）

⚠️ **触发器**：一旦本文件夹新增或文件架构逻辑调整，请立即重写此文档。

