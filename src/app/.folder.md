# Folder: /src/app

1. **"Status"**: Next.js App Router routes and pages layer, handling route navigation and page rendering, following Next.js conventions.
2. **"Logic"**: Each subfolder corresponds to a route, page.tsx is the page component, layout.tsx provides layout, data is passed through route parameters.
3. **"Constraints"**: Must follow Next.js App Router conventions, direct DOM manipulation in page components is prohibited, must prioritize server components.

## 🧩 Member List

- `/`: Home page (page.tsx)
- `/bento`: Bento showcase page
- `/bento/editor`: Bento editor (main feature page)
- `/showcase`: Showcase examples
- `layout.tsx`: Root layout, includes global styles and fonts
- `globals.css`: Global CSS and Tailwind configuration

⚠️ **Trigger**: Once files are added or architecture logic is adjusted in this folder, immediately rewrite this document.





