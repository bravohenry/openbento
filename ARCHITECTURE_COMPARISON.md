# 架构对比分析：Bento.me vs OpenBento

## 📊 核心架构差异

### 1. **卡片圆角 (Border Radius)**

#### Bento.me HTML 分析
从 HTML 结构和样式推断：
- **圆角大小**: 约 `27px` (从 iframe 内容和样式推断)
- **边框**: 1px 实线边框 + inset 内发光
- **背景**: 白色或平台特定颜色

#### 当前实现
```typescript
// src/bento/core/BentoCard.styles.ts
export const BENTO_RADIUS = 23  // 23px
```

**差异**: 
- ❌ **圆角小了 4px** (23px vs 27px)
- ✅ 边框样式已实现（1px + inset）

**需要修改**:
```typescript
// 修改 BENTO_RADIUS 从 23 改为 27
export const BENTO_RADIUS = 27
```

---

### 2. **卡片内边距 (Padding)**

#### Bento.me
- 从 HTML 结构推断：内边距约为 `24px`
- 内容区域使用绝对定位，距离边缘 `24px`

#### 当前实现
```typescript
export const BENTO_PADDING = 24  // ✅ 正确
```

**状态**: ✅ 已正确

---

### 3. **卡片背景色**

#### Bento.me
- 默认背景：**白色** (`#ffffff`)
- 平台特定背景色（如 LinkedIn: `#F0F5FF`, YouTube: `#FFF5F5`）

#### 当前实现
```typescript
// src/bento/core/BentoCard.styles.ts
export const bentoColors = {
  cardBackground: '#F5FAFE',  // ❌ 错误！应该是白色
}
```

**差异**: 
- ❌ **默认背景色错误** (`#F5FAFE` vs `#ffffff`)

**需要修改**:
```typescript
export const bentoColors = {
  cardBackground: '#ffffff',  // 改为白色
}
```

---

### 4. **边框样式**

#### Bento.me
- 外边框: `1px solid rgba(0, 0, 0, 0.08)`
- 内发光: `inset 0 0 0 1px rgba(255, 255, 255, 0.22)` (浅色模式)

#### 当前实现
```typescript
export const bentoBorder = {
  light: {
    border: '1px solid rgba(0, 0, 0, 0.08)',  // ✅ 正确
    boxShadow: 'inset 0 0 0 1px rgba(255, 255, 255, 0.22)',  // ✅ 正确
  },
}
```

**状态**: ✅ 已正确

---

### 5. **卡片尺寸系统**

#### Bento.me
从 HTML JSON 数据：
```json
"style": {
  "mobile": "2x2",
  "desktop": "2x2"
}
```

尺寸系统：
- `1x1`: 基础单元
- `2x1`: 横向双倍
- `1x2`: 纵向双倍
- `2x2`: 四倍
- `2x4`: 纵向四倍
- `4x2`: 横向四倍
- `4x4`: 十六倍

#### 当前实现
```typescript
export const bentoSizes: Record<BentoSize, {...}> = {
  '1x1': { width: 175, height: 175, columns: 1, rows: 1 },
  '2x1': { width: 390, height: 175, columns: 2, rows: 1 },
  '1x2': { width: 175, height: 390, columns: 1, rows: 2 },
  '2x2': { width: 390, height: 390, columns: 2, rows: 2 },
  'bar': { width: 390, height: 68, columns: 2, rows: 1 },
  // 扩展尺寸
  '2x3': { width: 390, height: 605, columns: 2, rows: 3 },
  '3x2': { width: 605, height: 390, columns: 3, rows: 2 },
  '4x2': { width: 820, height: 390, columns: 4, rows: 2 },
}
```

**状态**: ✅ 尺寸系统正确，但需要确认：
- 基础单元: `175px` ✅
- 间距: `40px` ✅
- 计算: `2x1 = 175*2 + 40 = 390px` ✅

---

### 6. **网格间距 (Gap)**

#### Bento.me
- 从布局推断：间距约为 `40px`

#### 当前实现
```typescript
export const BENTO_GAP = 40  // ✅ 正确
```

**状态**: ✅ 已正确

---

### 7. **Profile Section (左侧个人资料)**

#### Bento.me HTML 结构
从 HTML 推断：
- 头像尺寸: 约 `120px × 120px`
- 头像圆角: 约 `32px`
- 名称字体: 大号粗体
- 描述字体: 中等大小

#### 当前实现
```typescript
// src/bento/editor/ProfileSection.tsx
- 头像: `w-[120px] h-[120px] rounded-[32px]` ✅
- 名称: `text-[36px] font-bold` ✅
- 描述: `text-[16px]` ✅
```

**状态**: ✅ 基本正确

**需要检查**:
- 字体族: 应该使用 `Inter`
- 行高和字间距

---

### 8. **卡片内容布局**

#### Bento.me
从 HTML 推断卡片内容结构：
- 使用绝对定位 (`position: absolute`)
- 图标位置: 左上角，距离边缘 `24px`
- 标题位置: 根据尺寸动态计算
- 按钮位置: 底部，距离边缘 `24px`

#### 当前实现
```typescript
// src/bento/widgets/link/LinkWidget.tsx
// PlatformCardContent 使用绝对定位
<div style={{
  position: 'absolute',
  top: 24,
  left: 24,
  right: 24,
  bottom: 24,
}}>
```

**状态**: ✅ 布局方式正确

---

### 9. **图标样式**

#### Bento.me
- 图标容器: 圆角矩形 (`border-radius: 10px` 或 `12px`)
- 图标背景: 平台特定颜色或透明
- 图标尺寸: 根据卡片尺寸变化 (40px, 56px 等)

#### 当前实现
```typescript
// LinkWidget.tsx
const iconRadius = layout.iconSize >= 40 
  ? (layout.iconSize === 56 ? 12 : 10) 
  : 8
```

**状态**: ✅ 图标圆角逻辑正确

---

### 10. **Hover 效果**

#### Bento.me
- Preview 模式: 叠加 15% 灰色涂层 (`rgba(0, 0, 0, 0.15)`)
- Edit 模式: 可能显示编辑控件

#### 当前实现
```typescript
// BentoCard.tsx
backgroundColor: 'rgba(0, 0, 0, 0.03)',  // ❌ 太淡了！
```

**差异**: 
- ❌ **Hover 叠加层透明度太低** (3% vs 15%)

**需要修改**:
```typescript
backgroundColor: 'rgba(0, 0, 0, 0.15)',  // 改为 15%
```

---

## 🔧 需要修改的文件清单

### 优先级 1: 关键样式差异

1. **`src/bento/core/BentoCard.styles.ts`**
   - [ ] 修改 `BENTO_RADIUS`: `23` → `27`
   - [ ] 修改 `bentoColors.cardBackground`: `#F5FAFE` → `#ffffff`

2. **`src/bento/core/BentoCard.tsx`**
   - [ ] 修改 Hover overlay 透明度: `rgba(0, 0, 0, 0.03)` → `rgba(0, 0, 0, 0.15)`

### 优先级 2: 细节优化

3. **`src/bento/editor/ProfileSection.tsx`**
   - [ ] 确认字体族为 `Inter`
   - [ ] 检查行高和字间距

4. **`src/bento/widgets/link/LinkWidget.tsx`**
   - [ ] 确认图标圆角计算逻辑
   - [ ] 检查按钮样式

---

## 📐 尺寸对比表

| 属性 | Bento.me | 当前实现 | 状态 |
|------|----------|---------|------|
| 圆角 | `27px` | `23px` | ❌ 需要修改 |
| 内边距 | `24px` | `24px` | ✅ 正确 |
| 间距 | `40px` | `40px` | ✅ 正确 |
| 基础单元 | `175px` | `175px` | ✅ 正确 |
| 默认背景 | `#ffffff` | `#F5FAFE` | ❌ 需要修改 |
| Hover 叠加 | `15%` | `3%` | ❌ 需要修改 |
| 边框 | `1px rgba(0,0,0,0.08)` | `1px rgba(0,0,0,0.08)` | ✅ 正确 |
| 内发光 | `inset 1px rgba(255,255,255,0.22)` | `inset 1.5px rgba(255,255,255,0.18)` | ⚠️ 轻微差异 |

---

## 🎯 架构方式差异总结

### Bento.me 架构特点
1. **CSS Modules**: 使用样式模块化
2. **Iframe 渲染**: 编辑器使用 iframe 隔离
3. **数据驱动**: 通过 JSON 配置驱动渲染
4. **服务端渲染**: Next.js SSR

### OpenBento 架构特点
1. **Tailwind CSS**: 使用实用类名
2. **组件化**: React 组件系统
3. **类型安全**: TypeScript 类型定义
4. **客户端渲染**: Next.js App Router (Client Components)

### 关键差异
- **样式系统**: CSS Modules vs Tailwind (这是技术选择，不影响视觉效果)
- **渲染方式**: Iframe vs 直接渲染 (不影响最终样式)
- **数据流**: JSON 配置 vs TypeScript 类型 (不影响布局)

---

## ✅ 结论

主要需要修改的**视觉差异**（不是架构差异）：

1. ✅ **圆角**: 23px → 27px
2. ✅ **背景色**: #F5FAFE → #ffffff  
3. ✅ **Hover 效果**: 3% → 15% 透明度

这些是**样式参数**的调整，不涉及架构改变。架构方式不同是正常的（CSS Modules vs Tailwind），关键是**视觉效果要一致**。

